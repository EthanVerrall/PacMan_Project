#ifndef SCATTER_GHOST_TEXTURE_H
#define SCATTER_GHOST_TEXTURE_H

#include <stdint.h>

//Forward declaring to save space, in the case this
//needs to be included in mutiple files

extern const uint16_t scatter_ghost_array[64];

#endif //SCATTER_GHOST_TEXTURE_H